/*

In JavaScript, `"use strict";` enables strict mode, which enforces stricter error-checking and prevents the use of undeclared variables. It helps catch common mistakes and improves code security. Strict mode can also enable optimizations in some JavaScript engines for better performance.

*/



'use Strict';

let hasDriverLicennse = false;
const passTest = true;

if(passTest) hasDriverLicennse = true;
if(hasDriverLicennse) console.log('i can drive');

